<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.happy.custom.view">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CanvasDrawActivity" />
        <activity android:name=".activity.PieActivity" />
        <activity android:name=".activity.CanvasOperatorActivity" />
        <activity android:name=".activity.CanvasDrawTextPictureActivity" />
        <activity android:name=".activity.PathSimpleActivity" />
        <activity android:name=".activity.DemoActivity"></activity>
    </application>

</manifest>